[package]
name = "psp"
version = "0.1.0"
edition = "2024"

[dependencies]
handlebars = "5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
plotters = { version = "0.3", features = ["ab_glyph"] }
rand = "0.10.0"